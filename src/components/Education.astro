---
import { type Education } from '../data/resume'
type Props = Education
const { type, name, where } = Astro.props
---

<div>
  <h3 class='text-lg font-medium text-slate-700'>{type}</h3>
  <p class="text-sm">
    <span>{name}</span>
    <span class="text-neutral-500">{where}</span>
  </p>
</div>